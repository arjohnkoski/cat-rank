import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cat.rank',
  appName: 'cat-rank',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
